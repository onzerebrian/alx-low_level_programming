#include "main.h"

/**
 * main - the beginning of the program
 * Return: 0
 *
 */

void print_alphabet(void)
{
	char nn = 'a';

	for (nn = 'a'; nn <= z; nn++)
	{
		_putchar(nn);
	}
	_putchar('\n');
}
