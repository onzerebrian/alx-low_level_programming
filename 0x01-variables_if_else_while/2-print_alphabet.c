#include <stdio.h>

/**
 * main - This is where the program begins
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n;
	char m=' ';

	for (n = 'a'; n <= 'z'; n++)
	{
		putchar (n);
	}
	putchar(m);
	return (0);
}
